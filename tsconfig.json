{
  "compilerOptions": {
    "outDir": "./dist/",
//  Подсвечивает все места где не указан тип
//  Однако это не означает что теперь вовсе нельзя использовать any
    "noImplicitAny": true,
    "module": "es6",
    "target": "es5",
    "jsx": "react",
//  Компилятор будет обрабатывать не только TS файлы но и JS
    "allowJs": true,
    "moduleResolution": "node",
// esModuleInterop позволяет работать с пакетами которые использует
// common js как с обычными пакетами с помощью import require module.export
    "esModuleInterop": true,
// Позволяет использовать ипорт функций или других элементов которые экспортируются
// по дефолту без использования конструкции import * as
    "allowSyntheticDefaultImports": true
  },
  "ts-node": {
    "compilerOptions": {
      "module": "CommonJS"
    }
  }

}